org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric / Minecraft 1.21.1
minecraft_version=1.21.1
# Keep below 0.19: its sponge-mixin 0.17.4 declares @Redirect/@ModifyArg `at` as At[],
# so compiled mixins store `at` as an array — which MixinExtras on NeoForge (Sinytra
# Connector) casts to a single @At and crashes on. 0.16.14 matches fabric.mod.json's floor.
loader_version=0.16.14

# Mod
mod_version=1.0.0+1.21.1
maven_group=io.github.mockmock69401
archives_base_name=ftbq-linespacing
